spring:
  datasource:
    url: jdbc:mysql://localhost:3306/students?createDatabaseIfNotExist=true
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
server:
  port: 8082
  servlet:
    context-path: /student
